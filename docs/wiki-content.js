/**
 * Every wiki page, in both languages.
 *
 * <p>One file rather than one per language so a page can never exist in English and silently not exist in
 * Vietnamese: both strings sit on the same object, and a missing translation is visible here rather than
 * appearing as English text on a Vietnamese page.
 *
 * <p>Content is authored English-first and translated beside it. `body` is a small subset of Markdown the
 * renderer understands — headings, paragraphs, lists, tables, and fenced code — because the alternative was
 * a Markdown library the docs site does not otherwise need.
 */
window.OMNIPET_WIKI = {
  meta: {
    version: '3.0.0-SNAPSHOT',
    // Generated by tools/build-wiki-stats.mjs from the Gradle test results, so it cannot drift the way
    // the previous hand-typed "54 suites / 203 tests" did.
    statsFile: 'wiki-stats.json',
  },

  ui: {
    en: {
      brandTagline: 'Field Manual',
      searchLabel: 'Search the manual',
      searchPlaceholder: 'Search pages, commands, config keys…',
      noResults: 'Nothing matched. Try a command name or a config key.',
      onThisPage: 'On this page',
      languageLabel: 'Language',
      sections: {
        start: 'Getting started',
        player: 'For players',
        operator: 'For operators',
        reference: 'Reference',
      },
      editedNote: 'Documentation lives in the repository; every page cites the command or config key it describes.',
    },
    vi: {
      brandTagline: 'Sổ tay',
      searchLabel: 'Tìm trong sổ tay',
      searchPlaceholder: 'Tìm trang, lệnh, khoá cấu hình…',
      noResults: 'Không có kết quả. Thử tên lệnh hoặc khoá cấu hình.',
      onThisPage: 'Trong trang này',
      languageLabel: 'Ngôn ngữ',
      sections: {
        start: 'Bắt đầu',
        player: 'Cho người chơi',
        operator: 'Cho người quản trị',
        reference: 'Tra cứu',
      },
      editedNote: 'Tài liệu nằm trong repository; mỗi trang đều dẫn ra lệnh hoặc khoá cấu hình mà nó mô tả.',
    },
  },

  pages: [
    {
      id: 'install',
      section: 'start',
      icon: 'download',
      title: { en: 'Install and first run', vi: 'Cài đặt và lần chạy đầu' },
      summary: {
        en: 'Drop the JAR in, start once, and know which files appear.',
        vi: 'Đặt file JAR vào, chạy một lần, và biết những tệp nào sẽ xuất hiện.',
      },
      body: {
        en: `## Requirements

| Item | Value |
| --- | --- |
| Server | Paper 1.21+ |
| Java | 21 |
| Dependencies | None required |

Every integration is optional. OmniPet runs with no other plugin installed; Vault, PlayerPoints, LuckPerms, MythicLib, MythicMobs, and ModelEngine each add capability when present and are skipped when not.

## First run

1. Put \`OmniPet-<version>.jar\` in \`plugins/\`.
2. Start the server once. OmniPet writes its files and stops there — nothing is enabled that you did not configure.
3. Stop the server, edit \`plugins/OmniPet/config.yml\`, and start again.

## What appears

| Path | What it is |
| --- | --- |
| \`config.yml\` | Everything you tune. Written from the shipped defaults. |
| \`messages.yml\` | Every player-facing line, generated from the built-in defaults. |
| \`pets/\` | Pet definitions. Use \`/pet admin browse\` rather than editing by hand. |
| \`eggs/\` | The egg catalog. Saving a pet in the Studio writes one automatically. |
| \`data/\` | Player state, journals, and recovery records. Do not edit while running. |

\`messages.yml\` is generated from the code, so removing a key restores its default and deleting the file regenerates it. A broken value degrades that one line — it never stops the plugin.

## Verify it loaded

Run \`/pet\`. The hub opens with Vault, Hatch, Active slots, and Help. Staff additionally see the Studio tile.`,
        vi: `## Yêu cầu

| Mục | Giá trị |
| --- | --- |
| Server | Paper 1.21+ |
| Java | 21 |
| Phụ thuộc | Không bắt buộc |

Mọi tích hợp đều là tuỳ chọn. OmniPet chạy được khi không có plugin nào khác; Vault, PlayerPoints, LuckPerms, MythicLib, MythicMobs và ModelEngine chỉ bổ sung tính năng khi có mặt, và được bỏ qua khi không có.

## Lần chạy đầu

1. Đặt \`OmniPet-<version>.jar\` vào \`plugins/\`.
2. Chạy server một lần. OmniPet ghi các tệp của nó rồi dừng ở đó — không có gì được bật mà bạn chưa cấu hình.
3. Tắt server, sửa \`plugins/OmniPet/config.yml\`, rồi chạy lại.

## Những tệp xuất hiện

| Đường dẫn | Là gì |
| --- | --- |
| \`config.yml\` | Mọi thứ bạn điều chỉnh. Được ghi từ mặc định đóng kèm. |
| \`messages.yml\` | Toàn bộ dòng chữ người chơi thấy, sinh từ mặc định trong code. |
| \`pets/\` | Định nghĩa pet. Dùng \`/pet admin browse\` thay vì sửa tay. |
| \`eggs/\` | Danh mục trứng. Lưu pet trong Studio sẽ tự ghi một mục. |
| \`data/\` | Dữ liệu người chơi, sổ ghi và bản ghi khôi phục. Không sửa khi đang chạy. |

\`messages.yml\` được sinh từ code, nên xoá một khoá sẽ trả về mặc định, và xoá cả tệp thì nó tự tạo lại. Một giá trị sai chỉ ảnh hưởng dòng đó — không bao giờ làm plugin ngừng hoạt động.

## Kiểm tra đã nạp

Chạy \`/pet\`. Hub sẽ mở với Vault, Hatch, Active slots và Help. Nhân viên quản trị thấy thêm ô Studio.`,
      },
    },

    {
      id: 'player-guide',
      section: 'player',
      icon: 'user',
      title: { en: 'Playing with pets', vi: 'Chơi với pet' },
      summary: {
        en: 'Hatch an egg, activate a pet, level it, and release it.',
        vi: 'Ấp trứng, cho pet ra, lên cấp và phóng thích.',
      },
      body: {
        en: `## One command to remember

\`/pet\` opens the hub. Everything else is reachable from there.

## Hatching an egg

![Hatch flow](img/hatch-flow.svg)

There are two ways, and both work.

**From your hand.** Hold the egg and run \`/pet hatch main\`, or open \`/pet hatch\` and click the start button. The egg leaves your inventory once payment settles, and the menu shows a countdown.

![Placed egg](img/placed-egg.svg)

**On the ground.** Place the egg beside a heat source — a torch, a campfire, a lantern, lava. It incubates where it stands with a floating countdown above it. Break the block to take the egg back, exactly as you placed it.

Some eggs are picky. A fire-affinity pet — a dragon, a phoenix — needs lava on all eight sides, and the game tells you so if you place it somewhere else. If you take the heat away, the countdown pauses rather than continuing.

## Speeding it up

Accelerator items shorten the wait. Hold one and click the matching button in \`/pet hatch\`, or run \`/pet hatch use-main\`. An instant-hatch item finishes it outright.

## Your vault

\`/pet vault\` lists everything you own.

- **Left-click** a pet to bring it out or put it away.
- **Right-click** a pet to manage it: favourite, lock, reorder, feed, or release.
- **Right-click your pet in the world** to open that same screen.

Slot 46 cycles the sort order and slot 47 cycles the filter, so a large vault stays usable.

## Levelling

EXP candy raises a pet's level; a breakthrough stone raises its evolution once the level requirement is met. Both are used from the pet's management screen — right-click the pet, then click the item.

## Active slots

You start with one active pet. More slots are bought with \`/pet slot\`, which shows every currency your server accepts and their prices. OmniPet never picks a currency for you.

## Releasing a pet

Release trades a pet for rewards. The preview is frozen before you confirm, so what you see is what you get. A locked pet cannot be released — that is what the lock is for.`,
        vi: `## Một lệnh cần nhớ

\`/pet\` mở hub. Mọi thứ khác đều vào được từ đó.

## Ấp trứng

![Luồng ấp trứng](img/hatch-flow.svg)

Có hai cách, và cả hai đều dùng được.

**Từ tay.** Giữ trứng rồi chạy \`/pet hatch main\`, hoặc mở \`/pet hatch\` và bấm nút bắt đầu. Trứng rời túi đồ khi thanh toán hoàn tất, và menu hiện đồng hồ đếm ngược.

![Trứng đặt xuống](img/placed-egg.svg)

**Đặt xuống đất.** Đặt trứng cạnh nguồn nhiệt — đuốc, lửa trại, lồng đèn, dung nham. Trứng ấp ngay tại chỗ với đồng hồ nổi phía trên. Phá block để lấy lại đúng quả trứng đó.

Một số trứng khó tính. Pet hệ lửa — rồng, phượng hoàng — cần dung nham bao quanh cả tám hướng, và game sẽ nói rõ nếu bạn đặt sai chỗ. Nếu bạn lấy nguồn nhiệt đi, đồng hồ tạm dừng chứ không chạy tiếp.

## Tăng tốc

Vật phẩm tăng tốc giúp giảm thời gian chờ. Giữ nó và bấm nút tương ứng trong \`/pet hatch\`, hoặc chạy \`/pet hatch use-main\`. Vật phẩm nở tức thì kết thúc ngay.

## Vault của bạn

\`/pet vault\` liệt kê mọi pet bạn có.

- **Click trái** một pet để cho ra hoặc thu về.
- **Click phải** một pet để quản lý: yêu thích, khoá, sắp xếp, cho ăn, hoặc phóng thích.
- **Click phải chính pet của bạn ngoài thế giới** cũng mở đúng màn hình đó.

Ô 46 đổi thứ tự sắp xếp và ô 47 đổi bộ lọc, nên vault nhiều pet vẫn dễ dùng.

## Lên cấp

Kẹo EXP tăng cấp cho pet; đá đột phá tăng bậc tiến hoá khi đã đủ cấp. Cả hai dùng từ màn hình quản lý pet — click phải pet, rồi bấm vật phẩm.

## Ô pet hoạt động

Bạn bắt đầu với một pet hoạt động. Mua thêm ô bằng \`/pet slot\`, nơi hiện mọi loại tiền server bạn nhận và giá tương ứng. OmniPet không bao giờ tự chọn loại tiền thay bạn.

## Phóng thích pet

Phóng thích đổi pet lấy phần thưởng. Bản xem trước được chốt lại trước khi bạn xác nhận, nên thấy gì là nhận đúng thứ đó. Pet đang khoá thì không phóng thích được — đó chính là mục đích của khoá.`,
      },
    },

    {
      id: 'commands',
      section: 'reference',
      icon: 'terminal',
      title: { en: 'Commands and permissions', vi: 'Lệnh và quyền' },
      summary: {
        en: 'Every command, its permission, and which spelling to use.',
        vi: 'Mọi lệnh, quyền tương ứng, và nên gõ cách nào.',
      },
      body: {
        en: `## Two spellings, one feature set

| Spelling | Alias | Use |
| --- | --- | --- |
| \`/pet ...\` | \`/pets\` | Everything, players and administration alike. |
| \`/petadmin ...\` | \`/opadmin\` | Administration without the second word. |

\`/petadmin transactions\` and \`/pet admin transactions\` are the same command routed through the same code, so a permission or a behaviour cannot differ between them. Existing scripts keep working.

## Player commands

| Command | Permission | What it does |
| --- | --- | --- |
| \`/pet\` | \`omnipet.general\` | Opens the hub. |
| \`/pet vault [page]\` | \`omnipet.general\` | Opens the vault. Left-click toggles active, right-click manages. |
| \`/pet hatch [main\\|off\\|claim\\|refresh\\|use-main\\|use-off]\` | \`omnipet.general\` | Opens the hatch menu, or performs one action from it. |
| \`/pet slot\` | \`omnipet.general\` | Buys the next active slot, currency of your choice. |
| \`/pet skill <pet-uuid> <binding-id>\` | \`omnipet.general\` | Casts an active skill from a pet that is out. |
| \`/pet help [page]\` | \`omnipet.general\` | Lists only the commands you can actually use. |

## Administration

Every command below accepts an **online player name or a UUID** where it takes a player, and tab-complete suggests online names. A UUID is only required for an offline player.

| Command | Permission | What it does |
| --- | --- | --- |
| \`/petadmin browse\` | \`omnipet.admin.managepet\` | Opens Pet Studio to author pets. |
| \`/petadmin reload\` | \`omnipet.admin.reload\` | Reloads config, messages, and definitions. |
| \`/petadmin item <type> <player> [amount]\` | \`omnipet.admin.item\` | Gives accelerators, candy, or stones. |
| \`/petadmin egg give <player> <egg-id> [amount]\` | \`omnipet.admin.egg\` | Gives a hatchable egg. |
| \`/petadmin egg create <egg-id> <definition-id> [duration]\` | \`omnipet.admin.egg\` | Defines a catalog entry by hand. |
| \`/petadmin pet give <player> <definition-id>\` | \`omnipet.admin.petgive\` | Grants a pet, skipping incubation. |
| \`/petadmin hatch inspect <player>\` | \`omnipet.admin.inspect\` | Reads persisted incubation state. |
| \`/petadmin hatch reduce\\|set\\|complete\\|cancel <player> <incubation-uuid> …\` | \`omnipet.admin.manageegg\` | Adjusts or ends an incubation. Works on offline players. |
| \`/petadmin transactions menu\` | \`omnipet.admin.reconcile\` | Reconciles transactions from a clickable menu. |
| \`/petadmin transactions [limit] [cursor]\` | \`omnipet.admin.reconcile\` | The same list as chat, resumable from a cursor. |
| \`/petadmin reconcile <transaction-uuid> <decision>\` | \`omnipet.admin.reconcile\` | Applies one decision to one transaction. |
| \`/petadmin skill pending\\|rollback <player> …\` | \`omnipet.admin.skill\` | Inspects or rolls back a skill reservation. |
| \`/petadmin cultivation pending\\|review\\|recover <player> …\` | \`omnipet.admin.cultivation\` | Recovers an interrupted feed. |
| \`/petadmin release list\\|recover\\|reconcile …\` | \`omnipet.admin.release\` | Handles release reward delivery. |

## When to use the menu and when to type

The menu is faster for anything where the value you need is already on screen — reconciling a transaction, for instance, where clicking a row carries its UUID for you.

Type the command when you need to act on an **offline player**, when you are **scripting**, or when you have a **cursor or UUID from a log or a ticket** to paste. Those are the cases a menu cannot cover, which is why the commands remain.`,
        vi: `## Hai cách gõ, một bộ tính năng

| Cách gõ | Alias | Dùng để |
| --- | --- | --- |
| \`/pet ...\` | \`/pets\` | Mọi thứ, cho cả người chơi và quản trị. |
| \`/petadmin ...\` | \`/opadmin\` | Quản trị mà không cần từ thứ hai. |

\`/petadmin transactions\` và \`/pet admin transactions\` là cùng một lệnh chạy qua cùng một đoạn code, nên quyền và hành vi không thể khác nhau. Script cũ vẫn chạy bình thường.

## Lệnh người chơi

| Lệnh | Quyền | Làm gì |
| --- | --- | --- |
| \`/pet\` | \`omnipet.general\` | Mở hub. |
| \`/pet vault [trang]\` | \`omnipet.general\` | Mở vault. Click trái đổi trạng thái, click phải quản lý. |
| \`/pet hatch [main\\|off\\|claim\\|refresh\\|use-main\\|use-off]\` | \`omnipet.general\` | Mở menu ấp, hoặc thực hiện một hành động. |
| \`/pet slot\` | \`omnipet.general\` | Mua ô hoạt động tiếp theo, chọn loại tiền. |
| \`/pet skill <pet-uuid> <binding-id>\` | \`omnipet.general\` | Dùng kỹ năng của pet đang ra. |
| \`/pet help [trang]\` | \`omnipet.general\` | Chỉ liệt kê những lệnh bạn thực sự dùng được. |

## Quản trị

Mọi lệnh dưới đây nhận **tên người chơi đang online hoặc UUID** ở chỗ cần người chơi, và tab-complete gợi ý tên online. Chỉ cần UUID khi người chơi offline.

| Lệnh | Quyền | Làm gì |
| --- | --- | --- |
| \`/petadmin browse\` | \`omnipet.admin.managepet\` | Mở Pet Studio để tạo pet. |
| \`/petadmin reload\` | \`omnipet.admin.reload\` | Nạp lại config, messages và định nghĩa. |
| \`/petadmin item <loại> <người chơi> [số lượng]\` | \`omnipet.admin.item\` | Cấp vật phẩm tăng tốc, kẹo, hoặc đá. |
| \`/petadmin egg give <người chơi> <egg-id> [số lượng]\` | \`omnipet.admin.egg\` | Cấp trứng có thể ấp. |
| \`/petadmin egg create <egg-id> <definition-id> [thời gian]\` | \`omnipet.admin.egg\` | Tạo mục danh mục bằng tay. |
| \`/petadmin pet give <người chơi> <definition-id>\` | \`omnipet.admin.petgive\` | Cấp pet trực tiếp, bỏ qua ấp trứng. |
| \`/petadmin hatch inspect <người chơi>\` | \`omnipet.admin.inspect\` | Đọc trạng thái ấp đã lưu. |
| \`/petadmin hatch reduce\\|set\\|complete\\|cancel <người chơi> <incubation-uuid> …\` | \`omnipet.admin.manageegg\` | Điều chỉnh hoặc kết thúc một lần ấp. Dùng được với người offline. |
| \`/petadmin transactions menu\` | \`omnipet.admin.reconcile\` | Đối soát giao dịch bằng menu bấm chuột. |
| \`/petadmin transactions [giới hạn] [cursor]\` | \`omnipet.admin.reconcile\` | Cùng danh sách đó dạng chat, tiếp tục được từ cursor. |
| \`/petadmin reconcile <transaction-uuid> <quyết định>\` | \`omnipet.admin.reconcile\` | Áp một quyết định cho một giao dịch. |
| \`/petadmin skill pending\\|rollback <người chơi> …\` | \`omnipet.admin.skill\` | Xem hoặc hoàn tác một lượt giữ kỹ năng. |
| \`/petadmin cultivation pending\\|review\\|recover <người chơi> …\` | \`omnipet.admin.cultivation\` | Khôi phục lần cho ăn bị ngắt. |
| \`/petadmin release list\\|recover\\|reconcile …\` | \`omnipet.admin.release\` | Xử lý việc trao thưởng phóng thích. |

## Khi nào dùng menu, khi nào gõ lệnh

Menu nhanh hơn khi giá trị bạn cần đã hiện trên màn hình — ví dụ đối soát giao dịch, bấm vào một dòng là nó tự mang UUID theo.

Gõ lệnh khi cần tác động lên **người chơi offline**, khi **viết script**, hoặc khi có **cursor/UUID lấy từ log hay ticket** để dán vào. Đó là những trường hợp menu không làm được, và cũng là lý do các lệnh vẫn còn.`,
      },
    },

    {
      id: 'config',
      section: 'operator',
      icon: 'sliders',
      title: { en: 'Configuring OmniPet', vi: 'Cấu hình OmniPet' },
      summary: {
        en: 'Which section is strict, which is lenient, and what a typo costs.',
        vi: 'Mục nào nghiêm ngặt, mục nào khoan dung, và gõ sai thì mất gì.',
      },
      body: {
        en: `## Two kinds of section

![Strict and lenient config](img/config-strictness.svg)

This distinction matters more than any single key.

| Section | On a bad value |
| --- | --- |
| \`storage:\` | **Startup stops.** A mistake about capacity or a price is a mistake about someone's pets or money. |
| everything else | **Warns and falls back** for that key alone. A typo in a page size must never stop players using their pets. |

## storage — capacity and slots

\`\`\`yaml
storage:
  vault:
    baseCapacity: 30      # everyone gets this much room
    maxCapacity: 200      # hard ceiling; nothing raises capacity past this
  activeSlots:
    multiPetEnabled: true # false limits everyone to one active pet
    base: 1
    max: 5
    unlocks:
      "2":
        permission: ""    # empty means anyone may buy it
        costs: { VAULT: 25000, PLAYER_POINTS: 50 }
\`\`\`

Lowering a limit never deletes a pet. Pets above the new limit become read-only until capacity is restored.

Every currency you list is offered as a separate choice with its balance shown. Remove one to stop accepting it.

## items — appearance and effect

Each item takes an optional appearance block, so a resource pack can give it a model:

\`\`\`yaml
items:
  egg:
    material: DRAGON_EGG
    customModelData: 1001
    glint: true
    lore:
      - "<dark_gray>Season 3 exclusive</dark_gray>"
\`\`\`

Appearance is read **when the item is created**. A reload restyles the next one; eggs already held or mid-hatch keep the look they were made with, because their payment record describes that exact item.

## gui.menus — restyling menus

![Hub layout](img/hub-layout.svg)

\`\`\`yaml
gui:
  menus:
    hub:
      filler: BLACK_STAINED_GLASS_PANE
      buttons:
        vault: { material: CHEST, slot: 10 }
\`\`\`

Buttons are keyed by **name**, not slot. That is deliberate: the hatch tile is a different item when ready, incubating, and idle, so naming one material pins it to that look in every state rather than making you list them.

A move onto an occupied slot, or outside the menu, is refused with a warning naming the key — the button keeps its normal position rather than silently vanishing.

The vault accepts materials but not slots or size: its pet rows fill slots 0-44 and the paging arithmetic is built on that shape.

## What reloads and what does not

| Setting | \`/petadmin reload\` |
| --- | --- |
| \`messages.yml\`, \`gui.feedback.*\`, \`gui.menus.*\`, \`gui.help.linesPerPage\`, item appearance | **Live.** |
| \`gui.vault.petsPerPage\`, \`gui.studio.promptTimeoutSeconds\` | **Restart only**, and documented as such in the file. |
| \`runtime.*\` | **Restart only.** A reload warns you it ignored the change rather than pretending. |`,
        vi: `## Hai loại mục cấu hình

![Cấu hình nghiêm ngặt và khoan dung](img/config-strictness.svg)

Phân biệt này quan trọng hơn bất kỳ khoá đơn lẻ nào.

| Mục | Khi giá trị sai |
| --- | --- |
| \`storage:\` | **Dừng khởi động.** Sai về dung lượng hay giá là sai về pet hoặc tiền của người khác. |
| các mục còn lại | **Cảnh báo và dùng mặc định** cho riêng khoá đó. Gõ sai kích thước trang không bao giờ được làm người chơi mất pet. |

## storage — dung lượng và ô

\`\`\`yaml
storage:
  vault:
    baseCapacity: 30      # ai cũng có sẵn mức này
    maxCapacity: 200      # trần cứng; không gì vượt qua được
  activeSlots:
    multiPetEnabled: true # false giới hạn mọi người còn một pet hoạt động
    base: 1
    max: 5
    unlocks:
      "2":
        permission: ""    # để trống nghĩa là ai cũng mua được
        costs: { VAULT: 25000, PLAYER_POINTS: 50 }
\`\`\`

Giảm giới hạn không bao giờ xoá pet. Pet vượt giới hạn mới trở thành chỉ-đọc cho tới khi dung lượng được phục hồi.

Mỗi loại tiền bạn liệt kê sẽ là một lựa chọn riêng kèm số dư. Bỏ một loại đi là ngừng nhận loại đó.

## items — hình thức và hiệu ứng

Mỗi vật phẩm có một khối appearance tuỳ chọn, để resource pack gán được model:

\`\`\`yaml
items:
  egg:
    material: DRAGON_EGG
    customModelData: 1001
    glint: true
    lore:
      - "<dark_gray>Giới hạn mùa 3</dark_gray>"
\`\`\`

Hình thức được đọc **lúc vật phẩm được tạo ra**. Reload sẽ đổi kiểu cho vật phẩm tiếp theo; trứng đang giữ hoặc đang ấp giữ nguyên hình dạng lúc được tạo, vì bản ghi thanh toán mô tả đúng vật phẩm đó.

## gui.menus — đổi kiểu menu

![Sơ đồ ô hub](img/hub-layout.svg)

\`\`\`yaml
gui:
  menus:
    hub:
      filler: BLACK_STAINED_GLASS_PANE
      buttons:
        vault: { material: CHEST, slot: 10 }
\`\`\`

Nút được đặt theo **tên**, không theo ô. Đây là chủ ý: ô hatch là vật phẩm khác nhau khi sẵn sàng, đang ấp, và đang rảnh, nên đặt một material sẽ ghim nó ở hình đó trong mọi trạng thái thay vì buộc bạn liệt kê từng cái.

Chuyển nút vào ô đã có nút khác, hoặc ra ngoài menu, sẽ bị từ chối kèm cảnh báo nêu rõ khoá — nút giữ vị trí cũ chứ không âm thầm biến mất.

Vault nhận material nhưng không nhận ô hay kích thước: các hàng pet chiếm ô 0-44 và phép tính phân trang dựa trên hình dạng đó.

## Cái gì reload được, cái gì không

| Thiết lập | \`/petadmin reload\` |
| --- | --- |
| \`messages.yml\`, \`gui.feedback.*\`, \`gui.menus.*\`, \`gui.help.linesPerPage\`, hình thức vật phẩm | **Áp dụng ngay.** |
| \`gui.vault.petsPerPage\`, \`gui.studio.promptTimeoutSeconds\` | **Chỉ khi khởi động lại**, và được ghi rõ trong tệp. |
| \`runtime.*\` | **Chỉ khi khởi động lại.** Reload sẽ cảnh báo là đã bỏ qua thay đổi, chứ không giả vờ đã áp dụng. |`,
      },
    },

    {
      id: 'pet-studio',
      section: 'operator',
      icon: 'edit',
      title: { en: 'Creating pets', vi: 'Tạo pet' },
      summary: {
        en: 'Author a pet in the Studio and get it into a player’s hands.',
        vi: 'Tạo pet trong Studio và đưa tới tay người chơi.',
      },
      body: {
        en: `## Author it

\`/petadmin browse\` opens Pet Studio. Pick a tier, create a pet, and set its icon. Every chat field tells you its format, gives an example, and accepts \`cancel\`.

Rarity bands, stats, skills, and progression are all optional. A pet with none of them still works — it hatches against a single full-range band.

## Give it out

Saving writes \`eggs/<definitionId>_egg.yml\` for you, so:

\`\`\`
/petadmin egg give <player> <definitionId>_egg
\`\`\`

That is the whole path from an idea to a player holding the egg.

## If you change the tier afterwards

The Studio tells you the existing egg is still on the old tier and names the command that re-creates it. That matters: the hatch roller refuses an egg whose tier disagrees with its pet, and without the warning you would only find out when a player's hatch never completed.

The egg is never overwritten silently — you may have tuned its duration or candidate pool by hand.

## Testing without waiting

\`/petadmin pet give <player> <definitionId>\` puts the pet straight into a vault, skipping incubation entirely. It needs its own permission because it is stronger than handing out an egg.`,
        vi: `## Tạo pet

\`/petadmin browse\` mở Pet Studio. Chọn tier, tạo pet, và đặt icon. Mọi ô nhập chat đều nói rõ định dạng, cho ví dụ, và nhận \`cancel\`.

Dải độ hiếm, chỉ số, kỹ năng và tiến trình đều là tuỳ chọn. Pet không có gì trong số đó vẫn hoạt động — nó nở với một dải đầy đủ duy nhất.

## Phát cho người chơi

Khi lưu, hệ thống tự ghi \`eggs/<definitionId>_egg.yml\`, nên:

\`\`\`
/petadmin egg give <người chơi> <definitionId>_egg
\`\`\`

Đó là toàn bộ đường đi từ ý tưởng tới lúc người chơi cầm quả trứng.

## Nếu sau đó bạn đổi tier

Studio sẽ báo trứng cũ vẫn ở tier trước và nêu tên lệnh để tạo lại. Điều này quan trọng: bộ nở trứng từ chối quả trứng có tier khác với pet, và nếu không có cảnh báo thì bạn chỉ phát hiện khi lần ấp của người chơi mãi không xong.

Trứng không bao giờ bị ghi đè âm thầm — có thể bạn đã tự tay điều chỉnh thời gian hoặc danh sách ứng viên.

## Kiểm thử không cần chờ

\`/petadmin pet give <người chơi> <definitionId>\` đưa pet thẳng vào vault, bỏ qua hoàn toàn việc ấp. Lệnh này cần quyền riêng vì nó mạnh hơn việc phát trứng.`,
      },
    },

    {
      id: 'troubleshooting',
      section: 'operator',
      icon: 'help',
      title: { en: 'When something is wrong', vi: 'Khi có sự cố' },
      summary: {
        en: 'Symptoms, what causes them, and what to run.',
        vi: 'Hiện tượng, nguyên nhân, và lệnh cần chạy.',
      },
      body: {
        en: `## An egg will not hatch

**"That egg has no catalog entry."** The egg's ID has no file in \`eggs/\`. Create one with \`/petadmin egg create\`, or re-save the pet in the Studio.

**The hatch says it is queued and then nothing happens.** Check the server log. If you changed the pet's tier after its egg was made, the roller refuses the pairing — re-create the egg. The Studio warns about this when you save.

**The egg is still in the inventory.** The countdown will also be frozen. Check \`data/egg-escrow/\` for a record stuck at \`PREPARED\`, and use \`/petadmin hatch cancel\` to clear the incubation.

## A player cannot see their pet

Pets are intent, not entities, until the runtime renders them. Check that the pet is active in \`/pet vault\` — an active pet shows a lime marker — and that the player is under their active-slot limit.

## Money moved but the slot did not appear

This is what reconciliation is for. Run \`/petadmin transactions menu\`, find the row, and click it. The confirm screen states exactly what each decision does:

| Decision | Meaning |
| --- | --- |
| Charge confirmed | The player was charged. Keep the money, grant the slot. |
| No charge confirmed | The player was not charged. Grant nothing, close it. |
| Refund confirmed | Return the money and close it. |
| Sync retry | Only retry the external permission check. No money moves. |

OmniPet never guesses here. An ambiguous provider result is recorded as ambiguous and waits for you, because guessing wrong means taking someone's money.

## A menu looks wrong after editing config

Check the server log on startup or reload. A bad material name, an out-of-range slot, or an unknown button name each warn with the exact config key, and that key falls back to its built-in value rather than breaking the menu.

## Nothing makes a sound

\`gui.feedback.enabled: false\` is a master switch. Check it first, then the individual category — an operator may have silenced only one.`,
        vi: `## Trứng không nở

**"That egg has no catalog entry."** ID của trứng không có tệp trong \`eggs/\`. Tạo bằng \`/petadmin egg create\`, hoặc lưu lại pet trong Studio.

**Báo đã vào hàng chờ rồi không có gì xảy ra.** Xem log server. Nếu bạn đổi tier của pet sau khi trứng đã tạo, bộ nở từ chối cặp đó — hãy tạo lại trứng. Studio có cảnh báo việc này khi bạn lưu.

**Trứng vẫn còn trong túi đồ.** Đồng hồ cũng sẽ đứng yên. Kiểm tra \`data/egg-escrow/\` xem có bản ghi kẹt ở \`PREPARED\`, rồi dùng \`/petadmin hatch cancel\` để xoá lần ấp đó.

## Người chơi không thấy pet của mình

Pet là ý định, chưa phải thực thể, cho tới khi runtime dựng nó lên. Kiểm tra pet có đang hoạt động trong \`/pet vault\` — pet hoạt động có dấu màu xanh lá — và người chơi chưa vượt giới hạn ô hoạt động.

## Tiền đã trừ nhưng ô không xuất hiện

Đây chính là việc của đối soát. Chạy \`/petadmin transactions menu\`, tìm dòng đó, và bấm vào. Màn hình xác nhận nói rõ từng quyết định làm gì:

| Quyết định | Ý nghĩa |
| --- | --- |
| Charge confirmed | Người chơi đã bị trừ tiền. Giữ tiền, cấp ô. |
| No charge confirmed | Người chơi chưa bị trừ. Không cấp gì, đóng lại. |
| Refund confirmed | Trả tiền lại và đóng giao dịch. |
| Sync retry | Chỉ thử lại việc kiểm tra quyền bên ngoài. Không dịch chuyển tiền. |

OmniPet không đoán ở đây. Kết quả không rõ ràng từ nhà cung cấp được ghi là không rõ ràng và chờ bạn xử lý, vì đoán sai nghĩa là lấy tiền của người khác.

## Menu trông sai sau khi sửa config

Xem log server lúc khởi động hoặc reload. Tên material sai, ô ngoài phạm vi, hay tên nút không tồn tại đều có cảnh báo nêu đúng khoá cấu hình, và khoá đó trả về giá trị mặc định chứ không làm hỏng menu.

## Không có âm thanh nào

\`gui.feedback.enabled: false\` là công tắc tổng. Kiểm tra nó trước, rồi tới từng loại — có thể ai đó chỉ tắt một loại.`,
      },
    },
  ],
};
